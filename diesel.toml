# For documentation on how to configure this file,
# see https://diesel.rs/guides/configuring-diesel-cli

[print_schema]
file = "src/schemas.rs"
custom_type_derives = ["diesel::sql_types::QueryId","diesel::sql_types::SqlType", "std::fmt::Debug"]

[migrations_directory]
dir = "migrations"
